# grounded
Grounded is an AI powered system which identifies misinformation, clarifies uncertainties and scores short-form content on accuracy.

## Development
- Install dependencies: `npm install`
- Build TypeScript from `src` to `build`: `npm run build`
- OpenAI SDK is included via the `openai` package.
